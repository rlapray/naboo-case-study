pre-commit:
  parallel: true
  commands:
    verify:
      glob: "{src,e2e,scripts}/**/*.{ts,tsx,js,jsx}"
      run: npm run verify
      stage_fixed: true
    verify-test:
      glob: "src/**/*.{ts,tsx,js,jsx}"
      run: npm run verify:test

pre-push:
  commands:
    e2e:
      run: npm run e2e
